# Copy to terraform.tfvars (gitignored) OR inject the secrets as TF_VAR_* from
# ansible-vault, exactly like ../../terraform does. The non-secret values below
# already default to this homelab in variables.tf — only override if different.

# --- Secrets (required; prefer TF_VAR_pm_api_* over committing these) -------
# pm_api_url          = "https://192.168.1.103:8006/api2/json"
# pm_api_token_id     = "terraform@pve!provider"
# pm_api_token_secret = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# --- Optional overrides ------------------------------------------------------
# gpu_pci_id = "0000:2b:00"   # verify with: lspci -nn | grep -i nvidia
# gpu_worker = {
#   name           = "gpu-worker-1"
#   vmid           = 127
#   ip             = "192.168.1.127"
#   cores          = 6
#   memory         = 16384
#   boot_disk_size = 80
# }
