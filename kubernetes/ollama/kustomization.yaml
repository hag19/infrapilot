# Phase 2 LLM stack. Apply with:  kubectl apply -k kubernetes/ollama/
# Becomes an ArgoCD Application source in Phase 4.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai
resources:
  - namespace.yaml
  - ollama.yaml
  - open-webui.yaml
  - ingress.yaml
